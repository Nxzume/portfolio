/**
 * @deprecated Import from `../content` instead.
 * Kept so existing imports keep working during the content migration.
 */
export {
  site,
  about,
  focuses,
  sketches,
  score,
  contact,
  hero,
  projectsSection,
  projects,
  getProject,
} from '../content'

export type { FocusId, Project, Sketch, ProjectSection } from '../content/types'
